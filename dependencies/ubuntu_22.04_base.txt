# This file contains everything you need to compile this
# lava dependencies, needed to run LAVA

python3-pip
cmake

# clang/llvm
clang-tools-14
libclang-14-dev
llvm-14-dev

# btrace needs this for C-JSON
libcjson-dev

# These packages are for libodb, needed for the database backend
odb
libodb-dev
libodb-pgsql-dev

# libjsoncpp needed for fbi json parsing, maybe can delete after FBI is replaced?
libjsoncpp-dev

# I need this for making LavaTool
g++-10

# FBI needs this, but I think we can remove once Python swap is in
protobuf-compiler
libprotobuf-dev
libprotobuf-c-dev